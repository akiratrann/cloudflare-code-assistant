import "../resumable-stream-DqXxQVjf.js";
import { AIChatAgent } from "../index.js";
import * as agents from "agents";

//#region src/experimental/forever.d.ts
type Constructor<T = object> = new (...args: any[]) => T;
type AIChatAgentLike = Constructor<Pick<AIChatAgent, "scheduleEvery" | "cancelSchedule">>;
declare function withDurableChat<TBase extends AIChatAgentLike>(Base: TBase): {
  new (...args: any[]): {
    /**
     * No-op heartbeat callback. The schedule itself keeps the DO alive;
     * the callback doesn't need to do anything.
     * @internal
     */
    _cf_streamKeepAlive(): void;
    /**
     * Keep the DO alive during streaming.
     * Returns a disposer that cancels the heartbeat schedule.
     */
    keepAlive(): Promise<() => void>;
    scheduleEvery: <T = string>(intervalSeconds: number, callback: keyof AIChatAgent<Cloudflare.Env, unknown>, payload?: T | undefined, options?: {
      retry?: agents.RetryOptions;
    }) => Promise<agents.Schedule<T>>;
    cancelSchedule: (id: string) => Promise<boolean>;
  };
} & TBase;
//#endregion
export { withDurableChat };
//# sourceMappingURL=forever.d.ts.map